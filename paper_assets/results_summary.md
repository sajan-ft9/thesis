# Result Summary

_Results pending — run `make reproduce` (train → evaluate → quantize → benchmark →
explain → report) on the Kermany dataset to populate this file._

After a real run, `src/reporting.py` overwrites this with the measured headline
metrics (test AUC, accuracy, sensitivity, specificity, F1) for the primary model.
No numbers are written here until they are computed from real data.
